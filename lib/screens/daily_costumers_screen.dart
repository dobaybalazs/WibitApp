import 'package:flutter/material.dart';

class DailyCostumersScreen extends StatelessWidget {
  static const routeName = '/daily-costumers';

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mai vásárlók'),
    );
  }
}

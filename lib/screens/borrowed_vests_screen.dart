import 'package:flutter/material.dart';

class BorrowedVestsScreen extends StatelessWidget {
  static const routeName = '/borrowed-vests';

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Kölcsönzött mellények'),
    );
  }
}

import 'package:flutter/material.dart';

class ManageVestsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mellények kezelése'),
    );
  }
}
